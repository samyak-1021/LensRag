"""Evaluation harness — the differentiator.

Measures the exact failure generic PDF chat ignores: reading values off charts and
tables. Produces the text-RAG vs visual-RAG gap that proves the visual path earns
its keep. Run with ``python -m app.eval.runner``.
"""
